export default function AddCar() {
    return (
        <div>
            <h1>Add a New Vehicle</h1>
            <form>
                {/* We will build this form later! */}
                <input type="text" placeholder="Car Model" />
                <button type="submit">Submit</button>
            </form>
        </div>
    );
}